'use strict';

var expect = require('chai').expect;
var Interaction = require('../lib/commands/marionette/interaction.js');

describe('dalek-browser-firefox Command/Marionette/Interaction', function () {

  it('should exist', function () {
    expect(Interaction).to.be.ok;
  });

});

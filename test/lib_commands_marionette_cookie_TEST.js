'use strict';

var expect = require('chai').expect;
var Cookie = require('../lib/commands/marionette/cookie.js');

describe('dalek-browser-firefox Command/Marionette/Cookie', function () {

  it('should exist', function () {
    expect(Cookie).to.be.ok;
  });

});

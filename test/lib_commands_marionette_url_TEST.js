'use strict';

var expect = require('chai').expect;
var Url = require('../lib/commands/marionette/url.js');

describe('dalek-browser-firefox Command/Marionette/Url', function () {

  it('should exist', function () {
    expect(Url).to.be.ok;
  });

});

'use strict';

var expect = require('chai').expect;
var Url = require('../lib/commands/webdriver/url.js');

describe('dalek-browser-firefox Command/Webdriver/Url', function () {

  it('should exist', function () {
    expect(Url).to.be.ok;
  });

});

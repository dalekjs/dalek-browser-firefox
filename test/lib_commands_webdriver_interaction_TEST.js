'use strict';

var expect = require('chai').expect;
var Interaction = require('../lib/commands/webdriver/interaction.js');

describe('dalek-browser-firefox Command/Webdriver/Interaction', function () {

  it('should exist', function () {
    expect(Interaction).to.be.ok;
  });

});

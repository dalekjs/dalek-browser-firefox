'use strict';

var expect = require('chai').expect;
var Cookie = require('../lib/commands/webdriver/cookie.js');

describe('dalek-browser-firefox Command/Webdriver/Cookie', function () {

  it('should exist', function () {
    expect(Cookie).to.be.ok;
  });

});
